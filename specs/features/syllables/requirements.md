# 三拼音 — Requirements

## 資料

### R1：syllables.js 資料格式
```js
// SYLLABLE_TABLE[介音][聲母] = [可接的結合韻陣列]
export const SYLLABLE_TABLE = {
  'ㄧ': { 'ㄅ': ['ㄧㄝ', 'ㄧㄠ', ...], ... },
  'ㄨ': { ... },
  'ㄩ': { ... },
};

// 各介音系的有效聲母順序（表格行排列用）
export const INITIALS_FOR_MEDIAL = { 'ㄧ': [...], 'ㄨ': [...], 'ㄩ': [...] };
```

### R2：有效組合範圍
- ㄧ 系聲母：ㄅ ㄆ ㄇ ㄉ ㄊ ㄋ ㄌ ㄐ ㄑ ㄒ（10個）
- ㄨ 系聲母：ㄉ ㄊ ㄋ ㄌ ㄍ ㄎ ㄏ ㄓ ㄔ ㄕ ㄖ ㄗ ㄘ ㄙ（14個）
- ㄩ 系聲母：ㄐ ㄑ ㄒ ㄋ ㄌ（5個）
- 不存在於普通話的組合不顯示格子（顯示 —）

### R3：音訊
- ⚠️ **暫時停用**：尚未找到完整三拼音錄音來源，音訊功能待後續版本補入
- 原設計為使用 `playZhuyinSequence([聲母, ...結合韻符號])` 拼接三個 clip，但實際效果不佳，故暫不實作

## UI

### R4：頁面佈局
- 路由：`/syllables`
- Navbar 返回首頁（`/`）
- 三個分頁 tab：ㄧ 系 / ㄨ 系 / ㄩ 系，切換時清除 active 狀態

### R5：表格
- 欄標題：結合韻（兩個符號上下排列）
- 列標題：聲母
- 有效格子：顯示三個符號（聲母 + 結合韻兩個符號），可點擊
- 無效格子：顯示 —，不可點擊

### R6：格子樣式
- 與符號地圖的 `.symbol-circle` 一致：白底 + 3D 底部陰影，hover 上浮，active 下壓 + 黃底

### R6-B：置中大卡（WordPopover）
- 點擊有效格子後，**畫面中央**彈出大卡（modal 模式），背景加半透明遮罩
- 卡片寬 400px（手機為 `min(400px, calc(100vw - 48px))`），置中定位不需錨點量測
- 卡片內容：插圖區（240×240，Phase 1 佔位框）、中文詞（大字）、直式注音標記、EN/JP 譯文
- 卡片**右上角有關閉按鈕（✕）**
- 每個有效格子有獨立代表詞（`SYLLABLE_WORDS[聲母+結合韻]`）
- 點擊 ✕、點擊遮罩、或按 Escape 關閉；切換 tab 時自動關閉

> 歷史：原設計為錨定於按鈕旁的小浮動卡（176px），因插圖與注音太小改為置中大卡。

## 首頁入口

### R7：首頁第四張卡片
- 位置：四張 2×2 grid 中的左下角（第三格）
- 點擊導航至 `/syllables`
- 卡片圖片：`/三拼音.png`
- 四張卡片等高等寬：`grid-template-rows: 1fr 1fr`，grid 高度 `clamp(320px, calc(100svh - 200px), 500px)`

### R7-B：功能完成前鎖定（現況）
- 三拼音尚未完成（詞彙待審、插圖 122/156、音檔未做），首頁卡片暫時鎖定：
  - `disabled` + `pointer-events: none`，點擊無效
  - 圖片與標題 35% 透明度 + 去飽和，hover 無效果
  - 卡片中央顯示徽章：鎖頭 icon + `t.comingSoon`（EN: Coming Soon / JP: 準備中）
- 路由 `/syllables` 仍可直接訪問（開發測試用）
- 解鎖方式：移除 HomePage 按鈕的 `--locked` class、`disabled` 與 `.home-v2__card-lock` 徽章
